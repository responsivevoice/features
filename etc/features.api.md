## API Report File for "@responsivevoice/features"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { VoiceSelectorInput } from '@responsivevoice/types';
import { WebPlayerFeature as WebPlayerFeature_2 } from '@responsivevoice/types';
import { WebsiteFeatures } from '@responsivevoice/types';
import { WebsiteVoice } from '@responsivevoice/types';

// @public
export function createFeatureManager(): FeatureManager;

// @public
export class EndOfPageMessageFeature extends SpeakOnceFeature {
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "speakEndPage";
}

// @public
export class ExitIntentFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "exitIntent";
}

// @public
export interface Feature {
    readonly active: boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice, apiKey?: string): void;
    readonly name: string;
}

// @public
export class FeatureManager {
    activate(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice, apiKey?: string): void;
    cleanup(): void;
    get<T extends Feature = Feature>(name: string): T | undefined;
    getActiveFeatures(): string[];
    isActivated(): boolean;
    register(feature: Feature): void;
}

// @public
export class InactivityMessageFeature extends SpeakOnceFeature {
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "speakInactivity";
}

// @public
export class ParagraphNavigationFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "paragraphNavigation";
}

// @public
export type SpeakFn = (text: string, voice?: VoiceSelectorInput, params?: SpeakParams) => SpeakHandle;

// @public
export interface SpeakHandle {
    cancel(): void;
    pause(): void;
    resume(): void;
}

// @public
export class SpeakLinksFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "speakLinks";
}

// @public
export interface SpeakParams {
    onend?: () => void;
    onerror?: (error: Error) => void;
    onstart?: () => void;
    pitch?: number;
    rate?: number;
    volume?: number;
}

// @public
export class SpeakSelectedTextFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "speakSelectedText";
}

// @public
export class TabNavigationFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    readonly name = "accessibilityNavigation";
}

// @public
export class WebPlayerFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice): void;
    mount(selectorOrElement: string | HTMLElement, overrides?: WebPlayerMountOverrides): WebPlayerMountHandle | null;
    readonly name = "webPlayer";
}

// @public
export interface WebPlayerMountHandle {
    unmount(): void;
}

// @public
export type WebPlayerMountOverrides = Omit<Partial<WebPlayerFeature_2>, 'controls' | 'navigation' | 'layout'> & {
    controls?: Partial<WebPlayerFeature_2['controls']>;
    navigation?: Partial<WebPlayerFeature_2['navigation']>;
    layout?: Partial<WebPlayerFeature_2['layout']>;
};

// @public
export class WelcomeMessageFeature implements Feature {
    get active(): boolean;
    cleanup(): void;
    init(config: WebsiteFeatures, speak: SpeakFn, voice: WebsiteVoice, apiKey?: string): void;
    readonly name = "welcomeMessage";
}

```
